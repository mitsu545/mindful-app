# このリポジトリのルール

## 私について
- ジュニアデータアナリスト。プログラミングは初心者。日本語で話す

## 話し方
- 結論ファースト。専門用語は日常の例えに置き換える
- 長文より表・箇条書き。関係性は Mermaid で図解
- 推測で断定しない。不明は「わからない」。自信がない箇所は「⚠️未検証」

## 作業の進め方
- 実装前に「何をするか」を 3 行で提示し、OK を待つ
- コード修正は最大 2 回。3 回目は原因と対策を文章で説明してから
- 頼まれていない機能を足さない
- 出力前にセルフレビュー（要件 / エラー処理 / 過剰実装）し「✅レビュー済み」を添える

## ローカルでの動作確認手順を案内するとき（2026-09-13 の反省）
「設定ファイルを分ける」「拡張機能を入れる」など、初心者には複雑な方法を**先に**提案しない。
- まず `index.html` 1 ファイルだけで動作確認できる方法を最初に提示する（設定値は一時的にファイル内に直接書いてもらう。ただし GitHub にはコミットしない旨を毎回明記する）
- ブラウザで `file://`（ダブルクリックで開く）を使う場合、**別ファイル（`<script src>` で読み込む外部JSなど）は読み込めないことがある**。複数ファイル構成を提案する前にこの制約を先に伝える
- 「〇〇をインストールしてください」のような追加ツール導入は、他に方法がない場合の最終手段にする
- 手順は一度に全部出さず、1ステップごとに「ここまでできたら次」で進める

## やらないこと
- 本番データを変える DML（INSERT / UPDATE / DELETE）は提案まで。実行しない
- 秘密情報（トークン・パスワード）の値をチャット・コミットに出さない

## このプロジェクト固有
- **何を作るか**：マインドフルネス継続＋効果検証アプリ。詳細は `docs/spec.md` を**必ず最初に読む**
- 技術スタック：HTML / CSS / JavaScript（フレームワークなし）＋ Google Apps Script ＋ Google スプレッドシート
- 起動コマンド：なし（`index.html` をブラウザで開く）
- 公開先：GitHub Pages（private リポのため⚠️公開設定は要確認）
- 実行環境：Windows で開発、iPhone Safari で利用
- GAS の Web App URL と合言葉は `.env` に置き、コミットしない
